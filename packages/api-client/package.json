{
  "name": "@marginbase/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests --ext .ts",
    "test": "vitest run --passWithNoTests",
    "test:contract": "vitest run tests/contract.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
