{
  "name": "@marginbase/infra-aws",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/infra-validate.mjs --mode build",
    "lint": "node scripts/infra-validate.mjs --mode lint",
    "test": "node scripts/infra-validate.mjs --mode test && node --test tests/*.test.mjs",
    "typecheck": "node scripts/infra-validate.mjs --mode typecheck"
  }
}
